var React = require('react');

var Footer = React.createClass({

  // Used empty footer with CSS to get a purple stripe at bottom
  render: function(){
    return(
      <footer></footer>
    );
  }
});

module.exports = Footer;
